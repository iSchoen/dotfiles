# ~/.profile — sourced by login shells and uwsm env-preloader.
# Keep this lightweight and POSIX-compatible (no bashisms).

export MISE_DATA_DIR="$HOME/.local/share/mise"
export PATH="$HOME/.local/share/mise/shims:$PATH"
